function calculatingMathMean(a, b, c) {
    let sum = a + b + c;
    let arithmeticMean = sum / 3;
    console.log(`Среднеарифметическое чисел = ${arithmeticMean}`);
}
calculatingMathMean(10, 10, 10);

